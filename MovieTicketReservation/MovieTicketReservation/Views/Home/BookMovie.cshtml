@*@model MovieTicketReservation.Models.CustomerModel*@

@model MovieTicketReservation.Models.MoviesList
@{
//Layout = "Layout.cshtml";
}


<html>
<head>
    <link rel="stylesheet" href="~/css/BookMovie.css" />
</head>
<body>

    <div class="container">
        <div>
            <img src="~/Pikachu.jpg" class="movieImage" />
            <div class="section">
                @foreach (var employee in @Model.moviesList)
                { 
                    <h2>@employee.movieName</h2>
                <h6>Genre : @employee.movieGenre</h6>
                }
            </div>
        </div>
        @foreach (var employee in @Model.moviesList)
        {
            <div class="row">
                <div class="col-lg-3">
                    @*@Html.DisplayFor(model => model.Count())*@

                    <p>@employee.theatreModel.theatreName</p>
                    
                </div>
                <div class="col-lg-9">

                    <a href="@Url.Action("selectSeats", "Home",new { id=@employee.theatreModel.showtimingsModel.showId } )">
                    <button class="time" id="Cineplex1" onclick="Test(this)">@employee.theatreModel.showtimingsModel.timings</button></a>
                </div>
            </div>
        }
            <hr />
            <div class="row">
                <div class="col-lg-3">
                    Cineplex
                </div>
                <div class="col-lg-9">
                    <button class="time">:0.5 a.m</button>
                </div>
            </div>
            <hr />
            <div class="row">
                <div class="col-lg-3">
                    Cineplex
                </div>
                <div class="col-lg-9">
                    <button class="time">7:0.5 a.m</button>
                </div>
            </div>
            <hr />
            <div class="row">
                <div class="col-lg-3">
                    Cineplex
                </div>
                <div class="col-lg-9">
                    <button class="time">7:0.5 a.m</button>
                </div>
            </div>
        </div>
    <script>
        function Test(item) {
            console.log("hi");
            var itemId = item.id;
            var url = '@Url.Action("selectSeats/", "Home")';
            var data = { Id:itemId };
            $.post(url, data, function (result) {
            var id = '#postedFor' + postId;
            $(id).html(result);
            });
        }

    </script>
</body>

</html>


